# 铜产品减量化模型智能体 — 项目背景与技术要求

> 本文档用于开启新对话窗口时提供完整上下文。请先通读全文再开始工作；
> 涉及具体代码时，请以随本文档一起提供的 schemas.py / research_graph.py /
> reduction_graph.py / build_template.py 为准，本文档只讲设计意图和约束，
> 不重复贴代码。

---

## 一、项目背景

我是人工智能与环境科学与工程领域的交叉学者，正通过 LangGraph 开发专用
智能体，构建以下7种产品的铜减量化模型：

1. 光伏站
2. 建筑
3. 空调
4. 输配电变压器
5. 配电地下铜缆
6. 电动汽车（BEV/PHEV）
7. 风力发电机（海上/陆上）

**总体分析流程**：系统调研各产品主流型号 → 精确识别含铜部位 → 科学计算
单位铜强度 → 提出具有真实工程案例支持的铜减量化措施 → 基于措施绘制各产品
从目标年至2035年的单位铜强度变化路径。

构建方法可参照《产品铜件量化模型-手工版.xlsx》执行（并非绝对准则）。

### 起点问题

早期用通用大模型做简单提示词提问（如"调研光伏站的主流型号"），出现了
**概念层级混乱**问题：同一产品的"主流型号"可以从不同维度概括（如光伏板
可从电池技术维度分TOPCon/BC，也可从外形尺寸维度分1134mm矩形组件等），
不同维度下"型号"的定义完全不同。裸提示词无法让模型区分这些维度，容易
出现维度混杂、遗漏。因此需要建立 schema 和结构化的多agent流程，而不是
依赖裸prompt。

---

## 二、不可妥协的核心设计原则

以下原则是历次迭代中反复强调、且已落地为代码硬约束的部分，**新会话中
如果要修改代码，必须先确认改动不会违反这些原则**：

### 1. Schema避免硬编码人工先验知识

不能因为调研者本人知道"光伏站可分光伏板+BOS"就把这个划分写死进
schema。Schema只定义"型号需要从哪几类元级角度去发现"的**泛式**
（如 DimensionCategory 枚举：technology_route/physical_spec/
capacity_power/voltage_class/application_scenario/installation_method/
regional_standard/material_architecture/drivetrain_architecture/other），
具体产品有哪些维度、每个维度下有哪些取值，全部由智能体调研产生，不预置。

### 2. 两棵完全独立的树，绝不允许混淆

- **分类维度体系**（ClassificationDimension）：回答"型号怎么分类"
- **结构分解体系**（FunctionalSubsystem）：回答"产品由什么组成"

这两棵树在 schema 层面是不同的顶层类，字段互不共享；在 agent 层面由
DimensionAgent 和 StructureAgent 两个不同人设的角色分别负责，
StructureAgent 的 persona 里显式写了"如果你发现自己在写逆变器这种部件
名，说明你正在越界"。

### 3. 同轴一致性是P0级要求（用户反复强调的最高优先级检查）

同一个分类维度下的所有取值，必须严格落在该维度自己声明的分类轴上。
**反面案例**（真实发生过，来自手工版Excel和一次实测运行）：

- 手工版风电Excel：列头写"产品种类/技术路线"，但实际填的是"陆上"/
  "海上"——这是应用场景维度，不是技术路线维度
- 一次真实LangGraph运行结果：光伏站"核心技术设备类型（逆变器拓扑）"
  维度下混入了"光储一体机（储能逆变器）"——这个取值的定义特征是
  "是否集成储能"，跟"内部电路是集中式/组串式/微型拓扑"是两个不同的轴

**这类错误必须判定为blocking级别，不允许critic降级为warning**，因为
一旦发生，会让整个维度失去可计算意义（无法用它做单位铜强度的加权汇总）。

Schema层面的三道防线：

1. `ClassificationDimension.axis_of_variation`：维度自己先精确定义"取值
   该在哪个轴上变化"
2. `DimensionValue.axis_conformity_justification`：每个取值单独论证
   "我为什么符合这个轴"
3. `heuristic_axis_consistency_scan()`：代码层面的正则初筛（能抓物理规格/
   参数特征的字符模式，但**抓不住"光储一体机"这种纯语义层面的串轴**——
   这是实测中暴露的短板，说明光靠正则启发式不够，critic的LLM判断本身
   要更强，这是待改进方向之一，见第四节）

### 4. 多角色协作，不是"同一套prompt在数据分片上并行跑"

早期版本是"单agent+并行任务分发"（graph.py，已废弃），后升级为真正的
多角色协作架构：DimensionAgent / StructureAgent / CopperAgent /
ReductionAgent / QuantAgent 各有独立人设和职责边界，CriticAgent 是
**常驻且带跨轮记忆**的红队角色（不是流程末尾跑一次的校验函数），
SupervisorAgent 用"LLM决策 + 硬护栏校验 + 规则兜底"三层做路由，
agent间用 `Command(goto=..., update=...)` 自主交接，不用静态DAG边预先
画死路径。

Critic的跨轮记忆通过 `CriticReview`（`new_objections` +
`reopened_objection_ids` + `reopen_reasoning`）真正实现：critic能复核
"对方声称已回应的问题，回应是否真的成立"，不成立则重新打开该objection。

`Objection`/state中的`objections`字段用 `upsert_objections` 自定义
reducer按id去重覆盖，而不是简单追加，state任何时刻都是干净的最新列表。

### 5. 调研与减量化拆成两个独立的图，中间有版本化的、人工批准的边界

- **图A research_graph.py**：严格对应"调研型号+含铜部位"的范围，产出
  `ProductResearchOutput`，末尾是**强制人工审核关卡**（用
  `langgraph.types.interrupt()` 真正暂停，不是打个状态标记），支持
  "通过"和"打回给具体某个agent重做"两条路径。
- **图B reduction_graph.py**：只读消费经批准（`research_status= "approved"`）的调研成果，负责减量化措施+情景+路径建模，绝不能直接
  修改调研阶段的事实数据。若发现调研缺口，只能通过 `AmendmentRequest`
  提交修订请求，交由调研团队处理（不允许自己动手改数据）。

拆分理由：调研结果是"客观存在什么"，短期内不会变，应作为可版本化、可
复用的资产锁定；减量化措施/情景是"给定事实基础上做情景推演"，会反复
调整重跑，二者稳定性不同，不该耦合在一张图里、共享同一套审查标准。

### 6. 情景框架固定为S0-S3，语义统一，跨7种产品不可变

- S0「基准情景」：不纳入任何措施，Δmax恒为0
- S1「普通减量」/ S2「加速减量」/ S3「深度减量」

**S1/S2/S3的included_measure_ids之间不要求父子集关系**（S3不必包含S2
全部措施，因为措施可能互斥），但有硬约束：同一applicable_scope下必须
**严格递增** Δmax(S0)=0 < Δmax(S1) < Δmax(S2) < Δmax(S3)。这个约束：

- 在 `ProductReductionModel._scenario_severity_ordering` 做最终校验
  （含重复录入检测：同一子类别下同一scenario_id不能出现两次）
- 在 `reduction_graph.py` 的 `critic_agent` 做提前的确定性预检
  （`_scenario_ordering_hard_check`，逻辑与schema校验完全一致），
  不必等到最终整合才发现，quant_agent有机会在critic打回后重新分配

### 7. "行不用列"设计原则

产品内部的子类别差异（如风电陆上/海上、电动车BEV/PHEV）一律通过
`applicable_scope` 字段的不同**行**来表达，不通过新增专属**列**或
修改标题实现。适用于 `CopperReductionMeasure`、`Scenario`、
`IntensityTrajectoryPoint`，以及xlsx模板的Section 3/4/5/6。
反例：手工版Excel给风电的陆上/海上各开一套专属列（如"单位铜消耗绝对
降低量(陆上)"/"(海上)"），导致列数随子类别数量膨胀，7种产品无法共用
列结构。

### 8. 输出样式：Excel是纯结果展示，不是计算工具

按用户明确决定：后续计算全部在代码里做（quant_agent +
integration_node），Excel模板不含任何公式，Section 6 的强度路径数值
由程序算好后直接写入。原手工版的"7. 减量化计算参数"整段移除（不再需要
维护一张独立的计算底稿工作簿）。6个小节标题在7种产品间完全统一，不嵌入
产品专属的单位/基准值/措施编号范围。

---

## 三、当前架构文件清单

| 文件                   | 职责                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `schemas.py`         | 全部Pydantic领域模型：ClassificationDimension/FunctionalSubsystem/CopperComponent/CopperReductionMeasure/Scenario/IntensityTrajectoryPoint/Objection/CriticReview/ValidationFlag/ProductResearchOutput/ProductReductionModel/AmendmentRequest，以及`upsert_objections`、`heuristic_axis_consistency_scan` 等工具函数 |
| `research_graph.py`  | 图A：DimensionAgent/StructureAgent/CopperAgent/CriticAgent/Supervisor + 人工审核关卡，产出并锁定`ProductResearchOutput`                                                                                                                                                                                                |
| `reduction_graph.py` | 图B：ReductionAgent/QuantAgent/CriticAgent/Supervisor，只读消费approved的调研成果，产出`ProductReductionModel`                                                                                                                                                                                                         |
| `build_template.py`  | 生成《铜产品减量化模型_通用输出模板.xlsx》的脚本                                                                                                                                                                                                                                                                         |

已废弃、不再使用的文件：`graph.py`（早期单agent并行版）、
`multi_agent_graph.py`（调研+减量化未拆分前的合并多agent版）。

---

## 四、待改进方向（截至最近一次实测发现，按优先级排序）

### P0 —— 已发现真实bug，已修复，需要在新环境里重新验证

1. **阶段转移死锁**：`RESEARCH_STAGE_AGENTS["dimension_structure_debate"]`
   不含`copper_agent`，但supervisor需要在此阶段判断"该不该派copper_agent
   进入下一阶段"，导致LLM的正确决策被误判为越界、强制回退、永远无法
   推进到含铜部位调研阶段。`reduction_graph.py`的`reduction_research→ quantification`转换点是同样的bug。**修复方式**：在校验前动态构造
   `allowed_agents`集合，当"维度+结构提案均已产出且无blocking质询"
   （或"措施已产出且无blocking质询"）时才把下一阶段的agent加入合法列表。
2. **Checkpointer误用**：`SqliteSaver.from_conn_string(...)`返回的是
   上下文管理器，不能直接当checkpointer对象用。**修复方式**：改用
   `sqlite3.connect(db_path, check_same_thread=False)` 手动建连接后
   `SqliteSaver(conn)`直接构造。

   请在新会话中确认这两处修复已经落地到最新代码里，并做一次端到端
   dry-run（哪怕mock LLM返回也行）确认图能正常走完全程、不会卡死。

### P1 —— 实测内容质量问题，需要针对性加强prompt或校验

3. **Critic的P0检查在真实运行里漏检了**：光伏站"核心技术设备类型
   （逆变器拓扑）"维度混入"光储一体机"这个真实案例，critic没有拦下来。
   说明：
   - `heuristic_axis_consistency_scan`的正则启发式只能抓"数字+单位"
     模式的物理规格串轴，抓不住"光储一体机"这种纯语义层面的功能特征
     串轴——**需要考虑给启发式扫描增加语义层面的规则**，或者接受
     "语义类串轴只能靠LLM判断"的现实，转而**加强CRITIC_AGENT_PERSONA
     里P0检查的具体操作指引**（比如要求critic对每个取值都做一次
     "能否用这个维度的axis_of_variation原句造出一个通顺的判断句"的
     自测，通不过就是串轴）
   - 需要设计一套回归测试用例（把这次实测中真实出现过的串轴案例，
     以及风电陆上/海上案例，都固化成测试输入），验证改进后的critic能
     稳定抓住这两类已知错误模式，防止同类问题再次滑过
4. **含铜部位疑似过度识别**：实测中光伏组件下的玻璃、EVA封装材料、
   背板等材料全部被标记为含铜（这些材料在真实光伏组件里几乎不含铜），
   需要核实：
   - 是copper_agent的真实过度识别（为了"不遗漏"而对每个叶子节点都
     "找出点铜来"），还是前端UI渲染的展示bug（不论实际判定结果，统一
     给每个叶子节点套上"Cu·<节点名>"的标签样式）
   - 若确认是copper_agent的问题，`COPPER_AGENT_PERSONA`需要加强
     "没有把握就应明确判定不含铜，不要为了完整性硬凑"的措辞，甚至可以
     考虑要求每条CopperComponent的`citations`必须达到某个可信度门槛
     才能判定"含铜"，否则默认不含铜

### P2 —— 需要核实但优先级较低

5. `varies_by_dimension_ids`字段的实际使用情况：实测中"储能子系统"被
   作为光伏电站的顶层子系统列出，但储能并非所有光伏电站的标配（纯发电
   站可以没有储能）。需要检查这个字段有没有被正确填写来标注"该子系统
   依赖某个维度取值才存在"，而不是让结构树看起来暗示所有光伏电站都
   必须有储能。

## 六、给新会话的建议起始动作

1. 先让我（新的Claude实例）读一遍随附的 `schemas.py` / `research_graph.py`
   / `reduction_graph.py`，确认第四节列出的两个P0 bug修复已经在文件里
   （搜索 `allowed_agents` 和 `sqlite3.connect` 应该能找到）。
2. 针对P1第3点，讨论具体怎么改 `CRITIC_AGENT_PERSONA` 或
   `heuristic_axis_consistency_scan`，并设计一组回归测试固化已知的
   串轴案例。
3. 针对P1第4点，先去实际的JSON导出里核实"玻璃/EVA/背板"这几条
   `CopperComponent`记录的具体字段内容，再决定是加强prompt还是修前端。
