# 铜减量化模型智能体 — 新会话继续开发提示词

> 用法：在新聊天窗口中，把本文件全部内容作为第一条消息发送给 AI。

## 项目背景（自包含，请先阅读）

工作区：`/Volumes/Samsung SSD T5/铜减量化模型schema`，这是一个「铜产品减量化模型智能体」：
- 后端 FastAPI（`webapp/server.py`，端口 8000，`./webapp/start.sh` 启动）+ LangGraph 双图 + 前端单页（`webapp/static/`）
- 图A `research_graph.py`：分类维度 + 结构分解 + 含铜部位识别（含人工审核 interrupt 与区域证据关卡 region_evidence_gate）
- 图B `reduction_graph.py`：stage 0 基线量化（baseline_agent 提参数 / baseline_calc_node 确定性计算）→ 减量措施 → 情景参数（ScenarioParams，不含 Δmax）→ calculation_node 确定性计算 → integration 守护
- 在线模式：mimo-v2.5（OPENAI_BASE_URL 网关）+ Tavily 检索；无 Key 时 DemoLLM 演示模式
- 数据模板：`铜产品减量化模型_通用输出模板.xlsx`（6 小节，导出必须按它填数）；`5.GCAM Region.xlsx`（32 区域注册表来源）

## 核心架构原则（不可违背）

1. **LLM 只产出带引用的参数，一切数值由 `calculate.py` 确定性代码计算**（基线强度、Δmax、逐年路径、绝对降铜量），LLM 填的数字一律不采纳。
2. 每条事实必须带 Citation；证据不足的实体必须标 `region_basis="global_fallback"` 并说明，禁止臆断。
3. 所有防线必须保留：critic 审查、确定性硬检、integration 守护（缺产出即报错）、WatchdogLLM（420s 看门狗，应对 mimo 网关随机长尾挂起）、轮次耗尽兜底（代码生成保守情景，须通过 run_full_calculation 同链路自检）。
4. 失败必须诚实：报错要指出具体原因，绝不输出错误数据。
5. **发起任何整图在线运行（POST /api/runs、启动图A/图B）前必须先征得我批准**；代码修改与单元测试不受限。
6. 测试：`python3 -m pytest tests/ -q`，当前 35 项全绿，改代码必须保持全绿。
7. Excel 导出（`webapp/export_excel.py`）用"两遍法"构建 ops 序列填模板，**严禁 openpyxl 的 insert_rows**（不移动合并区域会错位）。

## 已完成并验证的内容（2026-09-10/11）

1. **确定性计算层**：基线强度 = Σ(部件质量×区域存在率)/归一化因子；情景 Δmax = 措施组合（同部件连乘、跨部件质量加权）；路径 = 基线×(1−Δmax×实现率)，linear/s_curve/step 闭式公式。
2. **区域区分度全链路**：区域注册表（中英检索别名/查询语言/国家归区）→ 区域化检索（region_hit/source_tier 标注）→ region_evidence_gate 五维评分（区域特异性40/权威25/多样性15/时效10/覆盖10，阈值70/40）→ 三档证据口径（region_specific/mixed/global_proxy）→ 无区域命中实体自动标注 global_fallback。
3. **模板填数导出**：六节结构、示例行删除、S6 路径由程序批量写入。
4. **前端**：区域 GCAM datalist 下拉、"区域与基线"结果 tab（证据评分卡 + 基线卡）。
5. **线上端到端已贯通**（输配电变压器 × 美国，run：图A `d775f81b6903` → 图B `4dea5a89b27b`）：
   - 图A：critic 拦截 P0 同轴错误并闭环修订；region_evidence_gate 实战（30.7分/insufficient→global_proxy）；绕组铜质量 68kg/72kg 真实量级。
   - 图B：基线 4.016 kg Cu/kVA（非零）；Δmax 由代码算出且严格递增（0 < 0.8964 < 34.0697 < 60.0129%）；44 个路径点数学一致；模板填数导出全部校验通过。
   - 期间修复并回归了 8 个线上缺陷（WatchdogLLM、维度感知部件匹配、unit_mass 强制、措施单调不减、轮次耗尽兜底、双轮保护、None update 防御、因子引用硬检降级）。

## 当前遗留问题（本次运行暴露，尚未解决）

1. **quant_agent 情景参数质量不稳定（最主要）**：本次 quant_agent 两轮产出的多子类别情景 Δmax 全相等（不同情景分配了相同措施组合），触发代码兜底——最终结果用的是 `_fallback_scenario_params` 生成的保守情景（按措施力度升序嵌套分配 S1/S2/S3，非 LLM 调研产物）。数学合法但分析保守。
2. **归一化因子引用质量弱**：baseline_agent 给出的换算因子大多缺 Citation（硬检已降级为 critic 语义层检查，否则流程必卡）。
3. **market_share 数值常为空**：dimension_agent 基本不填市场占有率数值（模板 Section 2 有此列），导致区域份额证据进不了导出与计算。
4. **部分部件 unit_mass 为 0**：本轮 11 个含铜部位中 7 个质量为 0（铁芯、油箱等），critic 打回一轮后仍不全——这些部件要么真不含铜（应显式说明）要么缺数据。
5. **amendment_requests 无闭环**：图B 提出的 3 条调研修订请求目前只记录不处理，图A 的局部重跑机制未与图B 打通。
6. **兜底情景在前端/导出无显著标识**：仅情景定义文本里注明，建议加徽章/列。
7. **region_hit 依赖文本匹配**：Citation.evidence_country 字段 LLM 填写率极低，区域特异性评分主要靠标题/URL 别名命中。

## 下一步优化建议（按优先级，请按此顺序推进）

**P1 — prompt 调优三件套（低成本高收益，先做）**
1. quant_agent 的 SCENARIO_DEFINITION_PROMPT 注入"单措施/嵌套组合试算表"：用 calculate.combine_scenario_delta 对每条措施及嵌套组合预先试算 Δmax，作为知情材料让 LLM 选择分配（Δmax 本来就由代码算，给试算表只是帮它选对组合，不违反确定性原则）。
2. copper_agent persona 已强制 unit_mass；追加确定性规则：质量为 0 的部件必须在 function_of_copper 中写明"不含铜/可忽略"理由，否则 critic 打回。
3. dimension_agent persona 强制市场占有率：每个取值必须尝试给出 market_share 数值+口径+年份+引用（检索关键词已含份额查询），确实无数据才允许留空并说明。

**P2 — amendment 闭环**：图B 的 amendment_requests 触发图A 局部重跑（图A已有 AmendmentRequest/research_status=amendment_in_progress 机制，打通调度即可）。

**P3 — 兜底情景可视化**：前端"情景定义"tab 对 fallback 情景加醒目徽章（如"系统兜底·非调研结论"），Excel Section 5 加"参数来源"列标注。

**P4 — 模型可用性**：mimo 网关有随机长尾挂起（WatchdogLLM 已兜底但拖慢节奏），支持多端点/模型降级配置。

**P5（二期）**：跨区域对比视图（同一产品多区域 run 的基线/路径并排）；region_hit 改为鼓励 evidence_country 填写的强 prompt + 评分加权。

## 验证方式

- 改代码后：`python3 -m pytest tests/ -q`（35 项，必须全绿）。
- 端到端：经我批准后 `POST /api/runs`（图A）→ 人工审核通过 → `POST /api/runs/{id}/start_reduction`（图B），检查：基线非零、Δmax 严格递增、44 路径点、导出 xlsx 六节齐全。
- 重点观察 P1 三项是否把 quant_agent 的兜底触发率降下来（成功标准：LLM 情景通过确定性校验，不触发 fallback）。
